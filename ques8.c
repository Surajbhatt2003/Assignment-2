#include <stdio.h>

int main()
{               int a; float b; char c; double d;
    printf(" Size of int is :%ld,\n size of float: %ld,\nsize of char: %ld,\nsize of double: %ld", sizeof(a),sizeof(b),sizeof(c),sizeof(d) );

    return 0;
}
